# 任务计划：WF 轮足双专家策略（轮态 / 足态）与高度命令

## 目标
只针对 WF_TRON1A 训练两个独立神经网络专家：轮态专家以持续滚动接触通过除上楼梯外的场景，足态专家硬约束轮速目标为 0、使用腿式步态通过全地形；两个专家都支持运行时机身高度命令，并由网络外的安全状态机完成模式切换。

## 当前阶段
旧版核心实现与 Wheel 大规模训练已完成；新版模态判断重构方案完成，代码实施待执行。

## 各阶段

### 阶段 1：需求收敛与 WF 现状分析
- [x] 将范围从 PF/SF/WF 收缩为仅 WF
- [x] 核对 WF 关节、执行器和动作空间
- [x] 核对现有 WF 地形、奖励和观测
- [x] 明确两个独立专家而非单网络条件策略
- [x] 明确轮态接触约束的物理可行边界
- **状态：** complete

### 阶段 2：兼容训练基线
- [x] 完成旧/新 API 兼容：目标 v2.2.1 + Sim 4.5，并已在当前 v2.3.2 + Sim 5.1 验证
- [x] 让新 Wheel/Foot 任务分别在 1 env、1 iteration 下生成 checkpoint（CPU PhysX）
- [x] 清理训练入口、Runner 与 Play 观测接口兼容问题
- **状态：** complete

### 阶段 3：双模态动作接口
- [x] 保留轮态 8 维动作：6 个腿关节位置 + 2 个轮关节速度
- [x] 足态保持统一 8 维输出，但环境 action term 与 FSM 双重将最后两维轮速动作硬 mask 为 0
- [x] 增加动作 mask/FSM 测试，保证足态策略无法驱动轮子
- [x] 统一部署侧模型输入、输出和电机命令映射文档
- **状态：** complete

### 阶段 4：轮态专家任务
- [x] 新增 WF-Wheel-Mode train/play Gym 任务
- [x] 构建无上楼梯的滚动地形课程：平地、上下坡、连续粗糙和低难度下楼梯
- [x] 增加轮地接触、腾空时长和轮速-机身速度一致性奖励
- [x] 抑制主动迈步：轮子离地时间和足端竖直速度
- [x] 对下楼梯采用“允许轮子失联但持续惩罚 air-time、保留恢复机会”的约束
- **状态：** complete

### 阶段 5：足态专家任务
- [x] 新增 WF-Foot-AllTerrain train/play Gym 任务
- [x] 接入 gait command、步态相位和摆动脚高度
- [x] 轮速度目标在 action term 和 FSM 中硬编码为 0，并保留实际轮速惩罚
- [x] 建立全地形课程：平地、坡面、连续粗糙、上/下楼梯
- [x] 增加接触时序、摆动轮净空和非轮部件碰撞奖励
- **状态：** complete

### 阶段 6：共享机身高度命令
- [x] 新增 BodyHeightCommand（相对局部地面，单位 m）
- [x] 将高度命令加入 observations.commands，由 Runner 拼接给 actor/critic
- [x] 为两个专家分别设置安全高度范围和变化速率
- [x] 将固定 0.80 m 高度奖励替换为命令跟踪奖励
- [x] 使用局部 height scanner 计算相对地面高度，并为两个专家统一观测 schema
- **状态：** complete

### 阶段 7：模式选择与安全切换
- [x] 首版使用网络外显式 mode command / 状态机，不训练第三个门控网络
- [x] 实现 Wheel→Foot：速度命令归零、轮驱动衰减、稳定检查和动作渐变
- [x] 实现 Foot→Wheel：稳定检查、双支撑条件和轮速渐增
- [x] 增加最短驻留时间和稳定条件；紧急停止接口留给实机控制层
- [x] 提供基于 height scan relief 的保守自动模式请求接口
- **状态：** complete

### 阶段 8：训练课程与验证
- [x] 分别完成 Wheel 和 Foot 的 1-iteration 训练链路验证（非可用策略）
- [ ] 加入高度随机化并验证动态高度跟踪
- [ ] 逐步加入坡面、粗糙地形和楼梯
- [x] 完成双 checkpoint Dual Play 10 步测试
- [ ] 大规模训练后统计接触率、轮速误差、高度误差、速度误差、成功率和能耗
- **状态：** in_progress

### 阶段 9：交付与部署文档
- [ ] 输出两个 checkpoint、JIT/ONNX 模型和标准化参数
- [x] 在 agent_docs/human 中记录模型命令、动作约束和运行方式
- [x] 给出 mode FSM 接口与实机安全限制
- [x] 补充训练与 Dual Play 命令；正式评测/导出待收敛 checkpoint
- **状态：** in_progress

### 阶段 10：物理模态判定与训练条件重构
- [x] 明确三类状态的物理语义：腾空态、固定姿态轮行态、步态足行态
- [x] 完成奖惩、终止、reset、课程与验收指标的详细调整方案
- [x] 删除 Wheel Expert 的双轮腾空硬终止，将 AIRBORNE 改为可恢复状态
- [x] Wheel 接触改为 4 帧力历史 + 几何置信度；不实现 gait 时间窗和 Reset 有效性筛选
- [x] 按轮行/足行专家拆分构型、接触、几何和时序奖惩
- [x] 实现空中运动奖励门控、落地冲击和支撑期滑移惩罚
- [x] 为新增 reward term 增加纯张量单元测试并完成 Wheel/Foot 环境冒烟验证
- [x] Reset 保持当前实现，不新增有效性筛选或拒绝重采样
- [ ] 增加模态标签、混淆区间和切换事件的训练日志与验收指标
- [ ] 在平地、坡面、粗糙地形和台阶上完成消融验证
- **状态：** in_progress

### 阶段 11：按新版模态判断方法重构
- [x] 对照外部最新版《模态判断方法》完成现有实现差异审查
- [x] 输出传感器、估计器、奖励、FSM、评测和测试的文件级修改方案
- [ ] 同步项目内模态定义文档，冻结 Wheel 合规信号接口
- [x] 新增左右轮地面过滤接触与 wheel-local 几何查询
- [x] 实现双轮接触门控、缺失接触惩罚和水平中性点惩罚，不实现 gait 时间窗
- [x] 用旧 model_20000.pt 完成水平中性点标定和 400 步新奖励兼容评测
- [x] 将 all-wheel 有效接触门控接入 Wheel reward；Dual Play FSM 暂不修改
- [x] 移除 axis-normal、z 差、轮端竖直速度和 xyz 相对速度奖励依赖
- [ ] 新增双专家 terrain feasibility 批量评测与四类地形标签
- [ ] 完成奖励消融训练和全地形重训
- **状态：** wheel_reward_complete_gpu_retrain_pending

## 关键语义
1. 轮态追求连续地形上的高轮地接触率；双轮腾空属于可恢复的 `AIRBORNE` 状态，通过 air-time、奖励门控和落地冲击抑制，不作为硬终止。
2. 足态“轮速为 0”是控制层硬约束，不只依靠奖励：网络保持统一 8 维输出，但环境 action term 和 FSM 都将最后两维硬置 0。
3. 两个模态是两个独立 actor/checkpoint；首版不使用单 actor 的 mode bit，也不使用可学习 gating network。
4. 模式切换由确定性状态机完成，避免两个专家在不同状态分布下直接瞬切。
5. 高度命令定义为 base/COM 相对局部支撑地面的高度。
6. 模态判定不直接等同于训练终止：腾空是可恢复状态，只用于惩罚、奖励门控和日志；轮行/足行是带时间窗与滞回的行为状态，避免单帧接触噪声引起标签抖动。
7. Wheel Expert 直接以“所有轮子有效触地 + 水平位置接近中性点”为合规目标；不在 Wheel reward 中实现 Foot gait 时间窗。Foot/Wheel 控制模式由外部 FSM 明确管理。

## 已做决策
| 决策 | 理由 |
|------|------|
| 仅训练 WF | 符合最新需求，减少无关工作和调参范围 |
| 两个独立专家网络 | 动作约束、地形能力和奖励目标差异明显，独立训练更稳定且便于安全验证 |
| 两专家保持相同 8 维 I/O；足态最后两维在环境与 FSM 中双重硬 mask 为 0 | 兼顾轮速安全约束和双 checkpoint 在同一环境中的无缝切换 |
| 首版使用外部 FSM 选模态 | 比第三个学习型门控网络可解释、可验证，适合实机安全控制 |
| 两专家共享 height scan、gait 和命令 schema | 保证两个 checkpoint 可在同一 Dual Play 环境无缝切换；Wheel Expert 可学习忽略无关 gait 输入 |
| 高度命令加入 commands 组，不进入 history encoder | 符合现有 Runner 拼接结构，避免重复输入 |

## 遇到的错误与风险
| 问题 | 处理方案 |
|------|---------|
| 普通 Python 导入 IsaacLab sensor 会缺少 `carb` | 直接读 2.2.1 源码；完整导入/环境验证必须通过 AppLauncher |
| 当前 Isaac Lab v2.3.2 与项目旧 API 不兼容 | 实施前固定 v2.2.1 + Isaac Sim 4.5.0，或先完成迁移 |
| 当前 WF 奖励惩罚 wheel joint velocity，不适合轮态 | 按模态拆分奖励；轮态删除该惩罚，足态强化实际轮速误差 |
| 当前 WF 没有 gait command | 足态任务接入 PF/SF 已有 gait command 机制并增加脚高跟踪 |
| 现有轮执行器 damping=0.8，单靠被动阻尼不能保证足态轮速为 0 | 使用固定零速度闭环目标，并通过电机力矩/速度限值验证 |
| 严格双轮永不离地与下楼梯存在物理冲突 | 不使用腾空硬终止；通过接触率、air-time、运动奖励门控和落地冲击约束主动跳跃 |
| 两专家必须维持相同 I/O schema | 已统一为相同观测、history、commands 和 8 维动作，并用 checkpoint shape 测试确认 |

## 备注
- 核心环境、训练、FSM、Dual Play 和文档已经实现。
- 当前生成的 checkpoint 仅用于链路冒烟，不具备有效运动能力；下一步是 GPU 大规模训练和奖励调参。
